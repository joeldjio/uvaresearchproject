# Core (required)
pymavlink>=2.4.40
pyserial>=3.5

# GCS UI — PySide6 (Qt for Python) + QML + Leaflet map
# Using PySide6 (LGPL v3) instead of PyQt6 (GPL v3) for commercial compatibility
# QML and WebEngine support included in PySide6-Addons
PySide6>=6.6
PySide6-Addons>=6.6

# Legacy QWidget UI (--legacy flag only, optional)
# pyqtgraph>=0.13

# Development & Benchmarking
pytest>=7.0
psutil>=5.9  # For CPU/memory monitoring in benchmarks
